{
  "name": "just-preview-example-configured-project",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && echo configured-project example > dist/README.txt",
    "typecheck": "echo configured-project typecheck",
    "lint": "echo configured-project lint",
    "test": "echo configured-project test"
  }
}
